{
  "name": "gh-close-test-files",
  "version": "0.2.0",
  "private": true,
  "description": "A small Chrome extension that marks GitHub test files as viewed.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run typecheck && npm test && npm run build",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "linkedom": "^0.18.13",
    "typescript": "^5.9.2"
  }
}
